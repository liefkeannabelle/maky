// file: src/syncs.ts
// Bridge file so imports like `import syncs from "@syncs"` work.
// It simply re-exports the auto-generated syncs barrel.

import syncs from "./syncs/syncs.ts";

export default syncs;
